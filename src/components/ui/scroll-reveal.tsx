"use client";

// Scroll reveal is now handled purely by CSS @keyframes animation
// This component exists for backward compatibility but does nothing
export function ScrollReveal() {
  return null;
}
